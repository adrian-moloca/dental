/**
 * Event Envelope Module
 *
 * Exports event envelope and metadata types for wrapping domain events
 * with infrastructure concerns.
 *
 * @module shared-events/envelope
 */

export { EventMetadata } from './event-metadata';
export { EventEnvelope, TenantContext } from './event-envelope';
